#include <stdio.h>
int main() {
 int amt, notes[]={500,200,100,50,20,10,5,2,1}, i;
 printf("Enter amount: ");
 scanf("%d",&amt);
 for(i=0;i<9;i++) {
 if(amt>=notes[i]) {
 printf("%d x %d\n", amt/notes[i], notes[i]);
 amt %= notes[i];
 }
 }
 return 0;
}
